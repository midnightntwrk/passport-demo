# ===========================================================================
# EVERY VARIABLE THIS APP READS — one line each
# ===========================================================================
# The sections below explain the ones with a decision behind them, at length.
# This index exists because that prose had grown past the point where you could
# tell whether a variable you were about to set was one of ours at all, and
# because several that the code reads had never been written down anywhere:
# VITE_BROWSER_PROVER, VITE_ENABLE_PWA_DEV, VITE_LOCALNET_DEMO,
# VITE_MIDNAMES_TLD_ADDRESS, VITE_MUSD_COLOUR_HEX, and both sponsor
# credentials. Kept in sync with `grep -ro 'VITE_[A-Z0-9_]*' src`.
#
# Everything here is OPTIONAL. Unset, the app is a stagenet build that proves
# in the browser, sponsors its fees through the stagenet gateway, and queues a
# name it cannot register — which is the configuration that is deployed.
#
# Network and endpoints
#   VITE_MIDNIGHT_NETWORK_ID     Which network the whole app follows. Default `stagenet`.
#   VITE_INDEXER_URL             Indexer GraphQL HTTP endpoint(s), comma-separated and ordered.
#   VITE_INDEXER_WS_URL          WebSocket endpoint(s), position by position against the list above. Derived when unset.
#   VITE_MIDNIGHT_NODE_URL       RPC node. Default the stagenet node.
#   VITE_MIDNIGHT_RELAY_URL      Submission relay. Derived from the node URL when unset.
#   VITE_DEEP_CHAIN_BLOCK_THRESHOLD  Ceiling on a from-genesis sync, in blocks. Default 1000000.
#
# Proving
#   VITE_MIDNIGHT_PROVING_URL    Proof server(s), comma-separated and ordered. Unset means prove in this tab.
#   VITE_MIDNIGHT_PROVING_URL_V3 ZKIR v3 proof server(s), same format. A SEPARATE list. Unset by default.
#   VITE_BROWSER_PROVER          `1` forces the in-tab prover even with a proof server configured.
#
# Sponsored fees and the Passport service
#   VITE_SPONSOR_URL             Fee gateway(s), comma-separated and ordered, or the literal `off`.
#   VITE_SPONSOR_API_KEY         CREDENTIAL — see the warning below. Sent as `X-API-Key`.
#   VITE_SPONSOR_CLIENT_ID       CREDENTIAL — see the warning below. Sent as `X-Client-ID`.
#   VITE_FUNDER_URL              Passport service(s) that register a name and open an account.
#
# What is listed in the Apps grid
#   VITE_RAFFLE_URL              Origin of the Midnight Raffle example. NO ENTRY AT ALL when unset in a production build.
#   VITE_RAFFLE_NAME             Relabels that entry. Default `Midnight Raffle`.
#   VITE_LOCAL_APP_URL           Origin of your own app, to see it in the grid. No entry when unset.
#   VITE_LOCAL_APP_NAME          Relabels that entry. Default `Local app`.
#   VITE_COMPANION_URL           The Midnight Companion chat. Must be https. A placeholder handle stands when unset.
#   VITE_TELEGRAM_URL            Support link in Home's footer. No link when unset.
#
# Demo and development only
#   VITE_LOCALNET_DEMO           `1` runs the mock claim and ungates Register now. Never set in a public build.
#   VITE_MIDNAMES_TLD_ADDRESS    Points name lookups at a locally deployed registry. Never set in a public build.
#   VITE_MUSD_COLOUR_HEX         Stablecoin colour for a build that knows it without asking. `/status` is preferred.
#   VITE_ENABLE_PWA_DEV          `true` registers the service worker under `npm run dev`, which it otherwise is not.
#
# ---------------------------------------------------------------------------
# THE TWO THAT ARE CREDENTIALS
# ---------------------------------------------------------------------------
# VITE_SPONSOR_API_KEY and VITE_SPONSOR_CLIENT_ID MUST NOT BE SET IN ANY BUILD
# THAT IS PUBLISHED. Every `VITE_`-prefixed variable is inlined by Vite into the
# JavaScript it emits — that is the whole mechanism — so a value set here is not
# configuration a server holds, it is a string in a file anyone who loads the
# page can read. Setting either of these publishes it.
#
# Nothing needs them. The stagenet gateway serves /wallet-status and
# /balance-only anonymously (verified 2026/08/31), and the deploy workflow sets
# neither. They exist for a developer pointing a LOCAL build at a gateway that
# does want a key, and for that they are set in a `.env.local` that is never
# committed and never built for deployment. A key that has reached a deployed
# bundle is disclosed and has to be rotated, not removed.
#
# ===========================================================================
# Dynamic environment id, from the Dynamic dashboard. Safe to expose to the
# browser — it names an environment, it does not authorise anything.
#
# UNSET IS THE SHIPPING CONFIGURATION. With no value here the app behaves
# exactly as it always has: the welcome screen offers the passkey and nothing
# else, Home's footer is unchanged, and no Dynamic code is fetched — the SDK
# lives behind an `import()` that the gate never reaches. Setting it adds a
# "Continue with Google, Microsoft, X, or Discord" entry beneath the passkey
# button and, once somebody signs in, an identity row on Home.
#
# It does NOT change how this Passport is authorised. The passkey on the device
# is still the key behind every call. See docs/demo/dynamic-integration.md for
# what this slice does, what it does not, and the dashboard settings an
# environment needs before the id here will do anything (social providers
# switched on, embedded Ethereum wallets created on sign-up, and this origin in
# Allowed Origins).
# VITE_DYNAMIC_ENVIRONMENT_ID=

# Midnight indexer GraphQL endpoint behind the mobile Home screen's recent
# transactions. Pass the HTTP URL — the client derives its own WebSocket URL.
# Defaults to the stagenet indexer when unset.
# VITE_INDEXER_URL=https://indexer.stagenet.shielded.tools/api/v4/graphql
#
# MORE THAN ONE INDEXER (2026/09/06). This takes a comma-separated, ORDERED
# list, on the same format VITE_SPONSOR_URL and VITE_MIDNIGHT_PROVING_URL take.
# A single URL is a list of one and behaves exactly as it always did.
#
#   VITE_INDEXER_URL=https://indexer.stagenet.shielded.tools/api/v4/graphql,https://indexer-2.example/api/v4/graphql
#
# The indexer is not chosen per request the way a proof server is. The wallet
# SDK is handed one indexer connection when the wallet is opened and holds it
# for that wallet's life, so the list is read at two moments:
#
#   1. WHEN A WALLET OPENS. Each endpoint is asked a one-block-height query
#      with a four-second ceiling, and the wallet is built against the first
#      that answers. A list of one skips the probe entirely.
#   2. WHEN THE OPEN ONE STOPS SERVING. A connection that drops, or a sync
#      percentage that has not moved for ninety seconds, rebuilds the wallet
#      against the NEXT endpoint in the list from the seed already stored —
#      the same rebuild a silent session restore performs, resuming from the
#      saved sync snapshot rather than walking the chain again. The list is
#      rotated rather than truncated, so a restarted primary is picked up
#      again without a redeploy, and the number of rebuilds in one session is
#      capped at the length of the list.
#
# Nothing about a failover reaches the screen: one console line names the
# endpoint index it moved to, and the wording the reader sees is unchanged.
#
# VITE_INDEXER_WS_URL, if you set it, follows this list POSITION BY POSITION
# rather than being a list in its own right — an indexer is one host reached
# two ways, and a pair that disagreed would subscribe to a different chain view
# than it queried. An override shorter than the HTTP list covers the endpoints
# it reaches and the rest derive theirs (HTTP URL, scheme upgraded, plus /ws),
# which is what makes adding a second indexer a one-variable change.

# Origin of the Midnight Raffle example dApp listed in the Apps registry
# (decided 2026/08/05 — replaces the deprecated VITE_ATLAS_URL). It must be a
# separate origin from Passport for the profile consent handshake to be real.
# VITE_RAFFLE_NAME relabels the grid entry the same way VITE_LOCAL_APP_NAME
# relabels the local one, and defaults to "Midnight Raffle".
#
# WHAT UNSET MEANS DEPENDS ON THE BUILD (2026/09/15). Under `npm run dev` it
# falls back to http://localhost:5177, the port examples/raffle-demo pins, so a
# plain local run still shows the handshake. In a PRODUCTION build there is no
# fallback and NO ENTRY AT ALL, because the old one shipped a card on a
# stranger's phone whose only destination was a port on their own machine —
# a tap opened the in-app browser on a connection that cannot be made, and
# nothing on the card could have told them why.
# VITE_RAFFLE_URL=http://localhost:5177
# VITE_RAFFLE_NAME=Midnight Raffle

# Generic local-development slot in the Apps grid (added 2026/08/06). Point it
# at any dev server and its app appears in the grid, labelled by
# VITE_LOCAL_APP_NAME or "Local app" when that is unset. This is the variable to
# hand a third-party developer; VITE_RAFFLE_URL above is the legacy name for the
# same mechanism and keeps working. Set both and both entries appear. Unset, the
# grid is exactly as it was. Same rule as the raffle: a separate origin from
# Passport, or the consent handshake is not real.
# VITE_LOCAL_APP_URL=http://localhost:5178
# VITE_LOCAL_APP_NAME=My App

# ---------------------------------------------------------------------------
# Local development origins — this app is pinned to port 5175
# ---------------------------------------------------------------------------
# The dev server binds http://localhost:5175 with `strictPort` (vite.config.ts),
# and src/main.tsx redirects any other origin to it whenever
# `import.meta.env.DEV` is set. Two consequences worth knowing before you lose
# an afternoon to them:
#
#   - if your browser lands on 5175 when you started the dev server elsewhere,
#     you did not mistype and nothing is broken — that redirect did it;
#   - a second dev server already holding 5175 collides with this one instead of
#     moving to the next free port, so start Passport first and leave 5175 to it.
#
# Nothing here overrides the pin; it is a constant in the source, deliberately,
# because Passport frames apps by URL and a handshake against a moving origin
# fails silently.

# Telegram support channel. When set, the Home screen renders a "Support on
# Telegram" pill in its footer area; when unset, no support link is shown.
# VITE_TELEGRAM_URL=https://t.me/midnightpassport

# The Midnight Companion chat, offered on the Apps tab and from Home's top bar.
# It must be an https URL; anything else is ignored and the built-in default
# stands. That default — https://t.me/MidnightCompanionBot — is a PLACEHOLDER:
# the real handle was not known when the control was written (2026/09/03), so
# any build that wants the working chat sets this.
# VITE_COMPANION_URL=https://t.me/MidnightCompanionBot

# ---------------------------------------------------------------------------
# The Passport service (optional)
# ---------------------------------------------------------------------------
# Base URL of a self-hosted passport-balancer or passport-funder service
# (examples/passport-balancer, examples/passport-funder). It does exactly two
# things for a Passport, and NEITHER of them puts value in the passkey wallet:
#
#   1. IT REGISTERS THE NAME. If the service's /status reports
#      aliasSponsorship "available" for this network, POST /register-alias
#      registers the .night name FOR the user — the user's Midnames key as
#      owner, their account-custody contract as target — paying the registry
#      price from its own NIGHT and the fees from its own DUST. The user's
#      wallet signs nothing and holds nothing; no balance of theirs is read at
#      any point in a claim. The client confirms the result with its OWN
#      registry read before reporting it confirmed. See
#      src/identity/sponsoredAlias.ts.
#   2. IT FUNDS THE ACCOUNT. Once the account-custody contract exists, POST
#      /fund-account proves a deposit_night — and, where the service holds one,
#      a shielded stablecoin deposit — against the CONTRACT. That is where the
#      opening balance goes, because the contract's own night_balances mirror
#      is what a withdrawal is checked against; NIGHT sent to the wallet
#      address instead would be money the Passport cannot see or spend.
#
# There is no activation drip and no self-paid claim behind either of these.
# The passkey wallet originates exactly one transaction in its life — the
# account-custody deploy — so a service that cannot register right now leaves
# the name QUEUED, and Passport says so plainly until it is on chain. Unset,
# every name queues.
#
# /fund-account is patient: the service reports itself SYNCING for a minute or
# two after its own spends, which is exactly when a freshly registered name
# asks it for a balance, so a 5xx or 429 is retried in the background (20 s,
# 40 s, 80 s, 160 s, 320 s) and an activation that never landed is picked up
# again on the next launch. Nothing about it can hold up or fail a name.
# VITE_FUNDER_URL=http://localhost:8799
#
# MORE THAN ONE SERVICE (2026/09/06). This takes a comma-separated, ORDERED
# list, on the same format VITE_SPONSOR_URL takes. A single URL is a list of
# one and behaves exactly as it always did.
#
#   VITE_FUNDER_URL=https://67-205-177-162.sslip.io/balancer,https://standby.example/balancer
#
# THE RULE HERE IS NARROWER THAN THE SPONSOR'S, and the difference matters. A
# proof recomputed by a second prover is the same proof and costs nobody
# anything. A second Passport service is a DIFFERENT WALLET, with its own
# NIGHT, its own DUST, and its own once-per-account record of who it has
# funded. So the list is fallen through ONLY when a service DID NOT ANSWER:
#
#   - the socket never opened, or the round trip timed out          → next
#   - 502, 503 or 504 with no service JSON in the body (the page a
#     reverse proxy sends when the process behind it is down)       → next
#   - any 2xx                                                       → stop
#   - any 4xx, 429 included                                         → stop
#   - 503 (or 502/504) carrying a JSON code such as wallet-syncing,
#     INSUFFICIENT_DUST or PAUSED — the service refusing for itself → stop
#   - 500 — that came out of the service's own process, which may
#     therefore have acted on the request before it fell over       → stop
#
# A refusal is an ANSWER, and the reader is shown it exactly as they are shown
# one service's refusal today. Asking the standby behind a refusal would
# register the same name from a second wallet (surfacing as name-taken for a
# name that is in fact theirs) or grant the activation a second time.
#
# THE ONE DOUBLE GRANT THIS CANNOT RULE OUT: the first service ACTED and its
# answer was lost coming back — it granted or registered, then the connection
# dropped or the edge timed out mid-proof. From the browser that is
# indistinguishable from a request that never arrived, so the standby is asked
# and may act again. It is bounded rather than prevented, by checks that were
# already there: the per-contract funding marker read before every attempt and
# after every backoff wait, the service's own `already-funded` answer being
# treated as a success, the hold that stops a grant while a name is unclaimed,
# and the account contract's own balance mirror, which makes a second grant
# visible and spendable rather than lost. Two services can over-fund one demo
# account; they cannot under-fund one or leave a Passport without a name.
#
# GET /status — the sponsorship probe and the stablecoin colour — is a read,
# so it simply takes the first service that answers.

# ---------------------------------------------------------------------------
# Choosing a network
# ---------------------------------------------------------------------------
# The whole app follows VITE_MIDNIGHT_NETWORK_ID: the wallet signs there, the
# network switcher opens there, names register there, and the faucet and
# explorer links point there. Unset means `stagenet`.
#
# There is only ONE value this build can transact on, and that is not a policy
# but a fact about the binary. Since 2026/08/24 the app is built on
# @midnightntwrk/ledger-v9 and midnight-js 5, because the ledger-8 stack cannot
# sync stagenet at all — its indexer client fails parsing the stagenet schema
# before the first block. Preview and Pre-production run the ledger-8 protocol,
# and a ledger-9 WASM module cannot decode their transactions or produce ones
# they will accept. Pointing the variables below at preview does not produce a
# preview build; it produces a broken one. src/lib/networks.ts keeps them as
# READABLE networks — an already-claimed name there still resolves and its
# transactions still link — and says so through networkUnavailableReason().
#
# VITE_MIDNIGHT_NETWORK_ID=stagenet
# VITE_INDEXER_URL=https://indexer.stagenet.shielded.tools/api/v4/graphql
# VITE_MIDNIGHT_NODE_URL=wss://rpc.stagenet.shielded.tools
#
# ---------------------------------------------------------------------------
# Proving
# ---------------------------------------------------------------------------
# Stagenet publishes NO proof server, so this is unset by default and that is a
# supported configuration rather than a gap. With it unset:
#
#   - the wallet's own balancing circuits are proved in this tab, by the
#     zkir-v2 worker in src/lib/wasmProver.ts. That needs the /zk-params tree
#     staged: `node scripts/fetch-zk-params.mjs`.
#   - contract circuits are proved in the same worker, through midnight-js's
#     own createProofProvider.
#
# Set it to run circuits on a server instead, which is faster. The matching
# image is 9.0.0-rc.6:
#
#   docker run -p 6300:6300 midnightntwrk/proof-server:9.0.0-rc.6
#
# VITE_MIDNIGHT_PROVING_URL=http://127.0.0.1:6300
#
# MORE THAN ONE PROOF SERVER (2026/08/31). This takes a comma-separated,
# ORDERED list. They are tried left to right, per request: the first one that
# answers proves the circuit, and one that fails or times out is fallen through
# to the next, with both facts logged (`[contract] prove … by <url> after
# <url>: <reason>`). A single URL is a list of one and behaves exactly as it
# always did.
#
#   VITE_MIDNIGHT_PROVING_URL=https://67-205-177-162.sslip.io/prover,https://api-stagenet.1am.xyz
#
# THE WALLET'S OWN CIRCUITS FAIL OVER TOO, since 2026/09/02. They did not
# before, and the reason they had to is the bug that hid behind the sentence
# that used to be here: `WalletFacade.init({ provingServerUrl })` reaches the
# SDK's own HTTP client, which builds `new URL('/prove', baseUrl)` — an
# ABSOLUTE path, so a base URL carrying a path loses it and
# `https://host/prover` becomes `https://host/prove`. On the deployed Caddy
# that address is a catch-all with no CORS header, so every wallet-side Zswap
# spend proof — the one `deposit_shielded` needs — died in a blocked preflight
# and was reported as a sponsor refusal. The facade is no longer given a
# `provingServerUrl` at all: `src/lib/walletProver.ts` builds the same
# per-request failover the contract path uses, so the path survives and a dead
# prover costs a retry rather than a transfer.
#
# NAMED BY ADDRESS, NOT BY A DNS RECORD (2026/09/02). The friendly name these
# examples used until today still resolves, but to a RECYCLED address — the
# droplet it named was deleted on 2026/08/27 and its IP reassigned — so it now
# reaches a host that is not ours. sslip.io encodes the address in the name and
# cannot go stale.
#
# ---------------------------------------------------------------------------
# A SECOND PROVING LIST, FOR ZKIR v3 (2026/09/16)
# ---------------------------------------------------------------------------
# VITE_MIDNIGHT_PROVING_URL_V3 takes the same comma-separated, ordered list and
# is read exactly the same way. It is not an extension of the list above and
# nothing falls back between them, because the two are not interchangeable in
# either direction.
#
# The `account-custody` module — the build of the account custody contract,
# which can carry a secp256k1 device beside a JubJub one — is compiled with
# --feature-zkir-v3.
# Neither of the usual places can prove those circuits:
#
#   - the in-tab prover is `@midnight-ntwrk/zkir-v2`, and it does NOT say so:
#     it fails with a key-format error, which reads as a bad artefact;
#   - the 1AM stagenet gateway is `ledger9-zkir2-dispatch`, and it is the
#     second entry in every deployed VITE_MIDNIGHT_PROVING_URL;
#   - `https://67-205-177-162.sslip.io/prover` is Caddy with the gateway first
#     and the droplet's own proof server as the fallback, so it is v2 first too.
#
# `/prover-v3` on the same droplet goes straight to that proof server —
# midnightntwrk/proof-server:9.0.0-rc.6, which has proved v3 circuits that
# stagenet then verified (txs 15ba523c… and e961946c…, 2026/09/16).
#
# UNSET IS A REFUSAL, NOT A FALLBACK. With no v3 server configured, anything
# that asks for the account custody module is refused by name rather than sent
# to a prover that cannot answer. Nothing in the app asks for it yet.
#
#   VITE_MIDNIGHT_PROVING_URL_V3=https://67-205-177-162.sslip.io/prover-v3
#
# WHAT THIS IS NOT, SINCE 2026/09/16. It is not where an account custody
# circuit is actually proved. `httpClientProofProvider` uploads the prover key
# with every request, and that build's keys are 3.2 GB — 224 MB per k256
# circuit — so no browser can carry that however healthy the server behind this
# URL is. A custody account proves by POSTing the SERIALISED TRANSACTION to the
# balancer, which already holds the keys on disk:
# `{VITE_SPONSOR_URL first entry}/prove-account-custody`. See
# `docs/demo/account-custody-layer-design.md` §7a.1 for the request and reply
# shapes. This variable keeps the job it was given — the switch that says a v3
# route exists at all — and nothing reads it for that traffic.
#
# ---------------------------------------------------------------------------
# THE DYNAMIC PASSPORT MILESTONE (2026/09/16)
# ---------------------------------------------------------------------------
# VITE_ACCOUNT_CUSTODY_MILESTONE=1 puts a developer screen on the page: the
# signed-in identity, and three buttons that create one of the new accounts,
# make the signed-in key the key that approves for it, and then make one
# approved call, each printing the transaction it produced.
#
# OFF BY DEFAULT, and off in every build shipped today. `?custody=1` on the URL
# does the same thing for somebody on a build that was not made for it. Both
# live INSIDE the VITE_DYNAMIC_ENVIRONMENT_ID branch in `src/main.tsx`, so a
# build with no social sign-in does not contain the screen at all — measured on
# 2026/09/16: the chunk is not emitted.
#
# It needs VITE_MIDNIGHT_PROVING_URL_V3 set (that is the switch above) and the
# balancer's `/prove-account-custody`, which is not deployed yet; without it the
# flow stops at the proving step and says so in one sentence.
#
#   VITE_ACCOUNT_CUSTODY_MILESTONE=1
#
# ---------------------------------------------------------------------------
# Sponsored fees
# ---------------------------------------------------------------------------
# ON BY DEFAULT: each network falls back to its own gateway when this is unset,
# because a fresh passkey wallet holds no DUST and cannot pay its first fee.
# On stagenet that gateway is OUR balancer — there is no 1AM gateway there —
# and it speaks the identical /wallet-status and /balance-only wire contract:
#
#   stagenet  https://67-205-177-162.sslip.io/balancer
#   preview   https://api-preview.1am.xyz
#   preprod   https://api-preprod.1am.xyz
#
# Set the literal value `off` to disable sponsorship, or a URL to use a
# different gateway. The client still gates on `available > 0` from the
# service's own /wallet-status, never on a hopeful assumption.
# VITE_SPONSOR_URL=off
#
# MORE THAN ONE SPONSOR (2026/08/31). This takes a comma-separated, ORDERED
# list too, on the same rule: the first endpoint whose /wallet-status reports
# `available > 0` is asked to balance, and one that refuses or fails is fallen
# through to the next. A single URL is a list of one and behaves exactly as it
# always did. If EVERY endpoint refuses, the user reads the sentence they read
# when one did; the endpoint names go to a console and never to a screen.
#
#   VITE_SPONSOR_URL=https://67-205-177-162.sslip.io/balancer,https://api-stagenet.1am.xyz
#
# The 1AM stagenet gateway serves /wallet-status and /balance-only anonymously
# (verified 2026/08/31 - a malformed body comes back 400 INVALID_TX, not 401),
# so VITE_SPONSOR_API_KEY is not needed for it.
#
# It could not prove for us on 2026/08/31 (`BALANCE_FAILED: Failed to prove
# transaction`, because its enclave wanted `midnight:ir-source[v3-generic]` and
# our Compact 0.33.0-rc.2 / zkir-v2 artefacts emit `ir-source[v2]`). 1AM shipped
# a `ledger9-zkir2-dispatch` build on 2026/09/01 and it now proves and balances
# for us; re-drilled the same day, every call a 200. It is still listed second
# while that fix earns some age - see deploy-demo.yml for the measurements and
# for the argument that eventually flips the order.
#
# When it IS busy it answers 429 with `A balance transaction is already
# pending.`, which `SponsorError.isPendingTransaction` recognises through its
# /already pending/i clause, so it is treated as waitable rather than as a
# refusal - and with two endpoints configured it is simply fallen through in
# about 140 ms.
#
# WHAT THIS VARIABLE DOES NOT COVER: the registration COST.
# `register_domain_for` runs receiveUnshielded(COIN_COLOR, COST), so whoever
# calls it supplies 600/140/10 atomic NIGHT (for a label of <=3/4/>=5 bytes)
# on top of the fee. That caller is never the user's wallet. The service at
# VITE_FUNDER_URL above registers the name and pays the COST from its own
# NIGHT; with no service available the name queues. Passport does not send a
# user to a captcha faucet to buy their own name, and no code path spends a
# user's NIGHT on one.
#
# ---------------------------------------------------------------------------
# The first sync, and why the depth guard exists
# ---------------------------------------------------------------------------
# A cold wallet walks the chain from genesis. Stagenet was ~158k blocks deep on
# 2026/08/24 and a fresh wallet synced in 13-52 s under Node, so this is a
# non-issue there today — but it is the reason Pre-production was never
# shippable, and the reason the guard in src/lib/localWallet.ts exists.
# Measured in-tab, on one machine, minutes apart:
#
#   preview   4% -> synced in ~75 s, heap steady at ~90 MB
#   preprod   3% after 150 s, heap climbing ~25 MB/s, tab CRASHES at ~4.2 GB
#
# RULED OUT 2026/08/06 — starting a NEW wallet at the chain tip.
#
# A wallet created in this app cannot have transactions older than itself, so
# it ought to be able to skip the walk. It was built and measured against the
# live preprod indexer: take the empty state the SDK serialises for a fresh
# wallet, move each component's `offset`/`appliedId` to the highest index its
# own subscription reports, and `restore()` it. The wallet opens and the facade
# reports SYNCED in ~3 s. Then the first real event arrives and the ledger
# throws:
#
#   values inserted non-linearly into zswap commitment tree;
#     expected to insert index 0, but received 17671.
#
# Both ZswapLocalState and DustLocalState hold a copy of a GLOBAL commitment
# tree that has to be filled from genesis in index order, and no public indexer
# endpoint serves anything that can fast-forward them. So skipping the walk
# needs state that only something which has already walked the chain can hand
# over — a service to build, not a variable to set. The code and the full
# write-up are in the tip-bootstrap section of src/lib/walletSnapshot.ts;
# nothing calls it.
#
# The guard itself refuses a from-genesis walk above VITE_DEEP_CHAIN_BLOCK_
# THRESHOLD blocks (default 1M) with an honest error, rather than starting one
# that kills the tab. Stagenet is far below it.
# VITE_DEEP_CHAIN_BLOCK_THRESHOLD=1000000
