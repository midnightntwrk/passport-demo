{
  "name": "mn-passport-foundations-demo",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Self-contained MN Passport foundations demo with passkey onboarding, identity registration, direct custody deposit, and restore flow.",
  "//workspaces": [
    "Added by the Passport app migration. The foundations demo at the repository",
    "root is NOT a workspace and is untouched by this list: it keeps its own",
    "`src/`, `test/`, `scripts/`, `tsconfig.json`, and the root dependencies",
    "below, exactly as it had them.",
    "",
    "`examples/passport-balancer`, `examples/passport-app-template`, and",
    "`examples/clubcoin-mock` are deliberately absent. They install and run",
    "standalone, as they did in the repository this was migrated from, and",
    "adding them would hoist their trees into a resolution that is already",
    "carrying two Midnight stacks (see //dependencies)."
  ],
  "workspaces": [
    "demo-backend",
    "packages/connect",
    "examples/passport-app-hub",
    "examples/passport-demo",
    "examples/passport-docs",
    "examples/passport-funder",
    "examples/passport-profile-client",
    "examples/raffle-demo"
  ],
  "//scripts": [
    "Every script the foundations demo shipped with is unchanged. The Passport",
    "app's scripts are added beside them, and the three whose names collided \u2014",
    "`build`, `test`, and `demo` \u2014 are prefixed `passport:`. Nothing else in",
    "this repository refers to them by name: the workflows added under",
    ".github/workflows call `npm run <script> --workspace <name>` directly."
  ],
  "scripts": {
    "lint": "eslint .",
    "compile": "compact compile contracts/account.compact contracts/managed/account && compact compile contracts/faucet.compact contracts/managed/faucet && compact compile contracts/identity_registry.compact contracts/managed/identity_registry",
    "build": "tsc",
    "demo": "node scripts/demo-local.mjs",
    "demo:e2e": "node app/scripts/e2e-devmode.mjs http://127.0.0.1:5173/",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "tsx src/tests/deploy.ts",
    "test:lifecycle": "tsx src/tests/lifecycle-night.ts",
    "test:shielded": "tsx src/tests/lifecycle-shielded.ts",
    "test:grants": "tsx src/tests/lifecycle-grants.ts",
    "test:recovery": "tsx src/tests/lifecycle-recovery.ts",
    "passport:build": "npm run build --workspace passport-demo-backend && npm run build --workspace passport-demo && npm run build --workspace passport-profile-client",
    "passport:test": "npm run test --workspace passport-demo-backend",
    "passport:demo": "npm run dev --workspace passport-demo",
    "test:demo-backend": "npm run test --workspace passport-demo-backend",
    "test:pwa": "npm run test:pwa --workspace passport-demo",
    "demo:profile-client": "npm run dev --workspace passport-profile-client",
    "demo:raffle": "npm run dev --workspace examples/raffle-demo",
    "demo:hub": "npm run dev --workspace passport-app-hub",
    "demo:docs": "npm run dev --workspace passport-docs",
    "deploy:passport:manual": "VITE_MIDNIGHT_NETWORK_ID=stagenet VITE_MIDNIGHT_PROVING_URL=https://67-205-177-162.sslip.io/prover,https://api-stagenet.1am.xyz VITE_SPONSOR_URL=https://67-205-177-162.sslip.io/balancer,https://api-stagenet.1am.xyz VITE_FUNDER_URL=https://67-205-177-162.sslip.io/balancer VITE_RAFFLE_URL=https://raffle.midnightpassport.com npm run build --workspace passport-demo && node scripts/build-vercel-output.mjs examples/passport-demo && cd examples/passport-demo && vercel deploy --prebuilt --prod --yes",
    "deploy:raffle": "VITE_PASSPORT_ORIGIN=https://midnightpassport.com npm run build --workspace examples/raffle-demo && node scripts/build-vercel-output.mjs examples/raffle-demo && cd examples/raffle-demo && vercel deploy --prebuilt --prod --yes",
    "deploy:template": "cd examples/passport-app-template && VITE_PASSPORT_ORIGIN=https://midnightpassport.com npm run build && cd ../.. && node scripts/build-vercel-output.mjs examples/passport-app-template && cd examples/passport-app-template && vercel deploy --prebuilt --prod --yes",
    "deploy:hub": "npm run build --workspace passport-app-hub && node scripts/build-vercel-output.mjs examples/passport-app-hub && cd examples/passport-app-hub && vercel deploy --prebuilt --prod --yes",
    "deploy:docs": "npm run build --workspace passport-docs && node scripts/build-vercel-output.mjs examples/passport-docs && cd examples/passport-docs && vercel deploy --prebuilt --prod --yes"
  },
  "//deploy:passport:manual": [
    "BREAK GLASS ONLY \u2014 `deploy:passport:manual` is not how the Passport demo",
    "ships any more. https://midnightpassport.com is deployed by",
    ".github/workflows/deploy-demo.yml, from a published GitHub release, and",
    "only from a commit that is an ancestor of origin/main. The rule the project",
    "lead asked for is that what is on main is what is deployed, and a laptop",
    "running `vercel deploy` is exactly what breaks it.",
    "",
    "It was renamed from `deploy:passport` rather than deleted, because GitHub",
    "Actions can itself be unavailable. What it does NOT do is the point: no",
    "typecheck, no unit tests, no PWA check, no end-to-end run, and no check",
    "that what it uploads corresponds to any commit \u2014 it ships whatever is on",
    "disk, uncommitted changes included.",
    "",
    "Its VITE_* values are duplicated in deploy-demo.yml's `env:` block, which",
    "is the copy that ships. Change both together.",
    "",
    "VITE_MIDNIGHT_PROVING_URL and VITE_SPONSOR_URL take a comma-separated,",
    "ORDERED list since 2026/08/31, tried left to right per request. The 1AM",
    "stagenet gateway is second and deliberately so; deploy-demo.yml carries the",
    "measurement that decided the order. VITE_FUNDER_URL stays a single URL:",
    "/register-alias and /fund-account exist nowhere but our balancer.",
    "",
    "docs/demo/deployment.md is the whole procedure: cutting a release, the",
    "three repository secrets, and rolling back with `vercel promote`.",
    "",
    "The other deploy:* scripts are unchanged \u2014 only the Passport demo has",
    "continuous deployment so far."
  ],
  "//lint": [
    "The whole tree at once. `eslint.config.js` at the root carries the rules,",
    "the two tiers, and the reasons; nothing is configured per workspace.",
    "",
    "It is NOT in CI yet, and that is deliberate rather than forgotten. The",
    "first full run reported 841 findings, so a gate added today would go red",
    "on every pull request for reasons that have nothing to do with the pull",
    "request. The order is: land the config, drive the count down, then add",
    "one step to .github/workflows/verify-demo.yml.",
    "",
    "`experiments/` is out of scope — see the note in eslint.config.js."
  ],
  "//dependencies": [
    "TWO CHANGES, and the test for both is that this repository's own gates give",
    "exactly the numbers they gave on `main`: `tsc -p tsconfig.json --noEmit`",
    "clean, and `npm test` 2 files / 23 tests. Both were measured against a fresh",
    "clone of `main` before and after, and both match.",
    "",
    "1. EVERY RANGE BELOW IS NOW EXACT, AND EVERY VERSION IS THE ONE `main`'s",
    "   LOCKFILE ALREADY RESOLVED. Not a single package moved: `^0.16.0` became",
    "   `0.16.0`, `^8.0.3` became `8.1.0`, `^4.0.4` became `4.1.1`, and so on,",
    "   each taken straight out of the committed lockfile on `main`. The ranges",
    "   had to be nailed down because this tree is bigger than it was, and a",
    "   caret resolves against whatever else is present: `^8.0.3` drifted to",
    "   ledger-v8 8.1.1 here while a nested consumer held 8.1.0, and two",
    "   ledger-v8 copies make `tsc` emit pages of 'ZswapSecretKeys is not",
    "   assignable to ZswapSecretKeys'.",
    "",
    "2. FOUR NAMES WERE ADDED \u2014 compact-js, midnight-js-protocol,",
    "   midnight-js-utils, and midnight-js-fetch-zk-config-provider \u2014 again at",
    "   the versions this repository already resolved transitively. They are",
    "   resolution control rather than code anything here imports.",
    "",
    "Why the four are needed. Two Midnight stacks live here at once: the root set",
    "below is LEDGER-8 (midnight-js 4.1.1, compact-runtime 0.16.0, ledger-v8",
    "8.1.0), shared by the foundations demo and `examples/passport-funder`;",
    "`examples/passport-demo` is on LEDGER-9 (wallet-sdk 2.0.0-beta.2,",
    "midnight-js 5.0.0-beta.6, compact-runtime 0.18.0-rc.1) because that is what",
    "stagenet runs.",
    "",
    "npm hoists one copy of each name to the root and nests the rest, and it",
    "hoists on FIRST CLAIM, not on merit. A ledger-9 package with no ledger-8",
    "counterpart at the root wins the root \u2014 and then every ledger-8 package that",
    "needed the ledger-8 version nests its own copy underneath itself. Measured",
    "on this tree without the four pins: compact-js 2.5.5-rc.7 sat at the root",
    "and six ledger-8 midnight-js packages each nested their own compact-js",
    "2.5.1, midnight-js-protocol 4.1.1, midnight-js-utils 4.1.1, and",
    "onchain-runtime-v3 3.0.0. That is what broke the foundations demo's",
    "typecheck, and it is what these four lines undo. With them, the root tree is",
    "byte-for-byte the shape `main` had, and there is exactly ONE copy of",
    "compact-runtime 0.18.0-rc.1 in the whole install \u2014 under the demo, where it",
    "belongs.",
    "",
    "Why duplicates are fatal rather than untidy. A generated Compact contract",
    "module opens with `import * as __compactRuntime from '@midnight-ntwrk/",
    "compact-runtime'` and then `__compactRuntime.checkRuntimeVersion('<exact",
    "version>')`. Node and the bundler both resolve that specifier from the",
    "MODULE's directory, walking upwards \u2014 so which copy a contract gets is",
    "decided by where the contract file sits, not by which workspace asked for",
    "it. And midnight-js decodes contract state through `ChargedState`: two",
    "copies of the runtime are two `ChargedState` classes, and an `instanceof`",
    "check then fails on objects that are correct in every other respect. Two",
    "copies of compact-js are two `CompactContext` TypeId symbols, and",
    "`deployContract` dies with `Cannot read properties of undefined (reading",
    "'ctor')` \u2014 measured against stagenet on 2026/08/24.",
    "",
    "So if you change anything here, re-run all four: this repository's `tsc` and",
    "`npm test`, and the demo's typecheck and `test:coverage`."
  ],
  "devDependencies": {
    "eslint": "^10.9.1",
    "eslint-plugin-react-hooks": "^7.1.1",
    "typescript-eslint": "^8.69.0",
    "@types/node": "22.19.20",
    "@types/ws": "8.18.1",
    "tsx": "4.22.4",
    "typescript": "5.9.3",
    "vitest": "3.2.6"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-js": "2.5.1",
    "@midnight-ntwrk/compact-runtime": "0.16.0",
    "@midnight-ntwrk/ledger-v8": "8.1.0",
    "@midnight-ntwrk/midnight-js-contracts": "4.1.1",
    "@midnight-ntwrk/midnight-js-fetch-zk-config-provider": "4.1.1",
    "@midnight-ntwrk/midnight-js-http-client-proof-provider": "4.1.1",
    "@midnight-ntwrk/midnight-js-indexer-public-data-provider": "4.1.1",
    "@midnight-ntwrk/midnight-js-level-private-state-provider": "4.1.1",
    "@midnight-ntwrk/midnight-js-network-id": "4.1.1",
    "@midnight-ntwrk/midnight-js-node-zk-config-provider": "4.1.1",
    "@midnight-ntwrk/midnight-js-protocol": "4.1.1",
    "@midnight-ntwrk/midnight-js-types": "4.1.1",
    "@midnight-ntwrk/midnight-js-utils": "4.1.1",
    "@midnight-ntwrk/wallet-api": "5.0.0",
    "@midnight-ntwrk/wallet-sdk-address-format": "3.1.2",
    "@midnight-ntwrk/wallet-sdk-dust-wallet": "4.1.0",
    "@midnight-ntwrk/wallet-sdk-facade": "4.0.1",
    "@midnight-ntwrk/wallet-sdk-hd": "3.0.2",
    "@midnight-ntwrk/wallet-sdk-shielded": "3.0.1",
    "@midnight-ntwrk/wallet-sdk-unshielded-wallet": "3.1.0",
    "@midnight-ntwrk/zswap": "4.0.0",
    "bip39": "3.1.0",
    "rxjs": "7.8.2",
    "ws": "8.21.0"
  },
  "//overrides": [
    "Added by the Passport migration. Three entries; each says what it changes",
    "and why it is safe.",
    "",
    "`vite` `^6.0.0`. The root's `vitest` reaches Vite through `vite-node`, whose",
    "range is `^5 || ^6 || ^7`, so npm hoists Vite 7 to the root and nests 6.x",
    "under each workspace that asks for `^6`. That is two Vite instances in one",
    "build: `@vitejs/plugin-react` binds to the hoisted 7 while the app runs 6,",
    "and `examples/passport-demo`'s service-worker stamping plugin then dies with",
    "`ENOENT \u2026 dist/sw.js` at `closeBundle`. Every workspace here already asks",
    "for `^6`; this only stops `vite-node` from dragging a second major in. The",
    "foundations demo's `app/` installs from its own lockfile and is untouched.",
    "",
    "`@midnight-ntwrk/wallet-sdk-utilities` 1.2.1. Nothing declares this",
    "directly \u2014 it arrives transitively. wallet-sdk 2.0.0-beta.2 pins 1.2.0, a",
    "publish that does not export `Clock`; wallet-sdk-facade imports `Clock`, so",
    "the wallet dies at build time with the rollup error 'Clock is not exported",
    "by \u2026/wallet-sdk-utilities/dist/index.js'. 1.2.1 is the fixed publish and is",
    "otherwise identical. It is written globally because both surgical forms were",
    "tried and neither reaches the copy inside",
    "node_modules/@midnight-ntwrk/wallet-sdk/node_modules, which is where the",
    "beta tree lives and where the broken copy is.",
    "",
    "`@scure/base` 2.3.0, SCOPED to the two address-format packages. A freshly",
    "resolved tree gives them 2.4.0, which reimposes bech32's classic",
    "90-character limit on bech32m. Midnight shielded addresses are 133",
    "characters, so `MidnightBech32m.parse` throws 'invalid string length 133,",
    "expected (8..90)' and the app reports a valid stagenet shielded address as",
    "'not a Midnight address'. Caught by e2e/send-assets.spec.ts and reproduced",
    "outside the browser before pinning; 2.3.0 is what the live site was built",
    "against.",
    "",
    "The scoping is the point. Written globally, it also moves @polkadot's copy",
    "off 1.2.6, and `@polkadot/util-crypto` imports a `utils` export that 2.x",
    "does not have \u2014 the build then fails with '\"utils\" is not exported by",
    "@scure/base'. So the two packages that need 2.3.0 get it and nothing else",
    "does."
  ],
  "overrides": {
    "vite": "^6.0.0",
    "@midnight-ntwrk/wallet-sdk-utilities": "1.2.1",
    "@midnight-ntwrk/wallet-sdk-address-format": {
      "@scure/base": "2.3.0"
    },
    "@midnightntwrk/wallet-sdk-address-format": {
      "@scure/base": "2.3.0"
    }
  }
}
